# Recipe: Nemotron-3-Nano-NVFP4 — Memory optimized for DGX Spark
# Based on sggin1's memory analysis (forums.developer.nvidia.com/t/364886)
# Memory: ~32GB | tok/s: ~50-58
recipe_version: "1"
name: Nemotron-3-Nano-NVFP4
description: vLLM serving Nemotron-3-Nano-NVFP4 — memory optimized for multi-model stack

model: nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4
container: vllm-node
solo_only: true

mods:
  - mods/nemotron-nano
env:
env:
  VLLM_USE_FLASHINFER_MOE_FP4: 1  # Allow this for Blackwell
  VLLM_NVFP4_GEMM_BACKEND: marlin
  # Delete FORCE_FP8_MARLIN possible conflict with NVFP4?

defaults:
  model: nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4
  port: 8000
  host: 0.0.0.0
  tensor_parallel: 1
  gpu_memory_utilization: 0.25
  max_model_len: 32768
  max_num_batched_tokens: 32768
  max_num_seqs: 8
command: |
    vllm serve {model} \
    --host {host} \
    --port {port} \
    --max-model-len {max_model_len} \
    --gpu-memory-utilization {gpu_memory_utilization} \
    --max-num-batched-tokens {max_num_batched_tokens} \
    --max-num-seqs {max_num_seqs} \
    --enforce-eager \
    --moe-backend cutlass \
    --kv-cache-dtype fp8 \
    --enable-prefix-caching \
    --load-format fastsafetensors \
    --trust-remote-code \
    --enable-auto-tool-choice \
    --tool-call-parser hermes \
    --reasoning-parser-plugin nano_v3_reasoning_parser.py \
    --reasoning-parser nano_v3


